import os

def SameDirs(my_dict):
    pass

def DeleteDirs(dir, disk_save_path):
    if CheckIfPathInDir(dir, disk_save_path): #build list without disk_save_path and _delete

def CheckIfPathInDir(dir, disk_save_path):
    pass


def main(my_dict, disk_save_path):
    dirs = SameDirs(my_dict)
    for dir in dirs:
        DeleteDirs(dir, disk_save_path)


if __name__ == '__main__':
    main()